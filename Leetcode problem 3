         left=0
         b=0
         venu=set()
         for right in range(len(s)):
             while s[right] in venu:
                 venu.remove(s[left])
                 left+=1
             venu.add(s[right])
             b=max(b,right-left+1)
         return b 
